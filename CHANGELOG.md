# Changelog

All notable changes to Northstar will be documented in this file.

## [0.1.0-alpha.63] - 2026-07-16

### Added
- **帳本可刪除**：帳戶頁的「帳本管理」每一列新增刪除按鈕。帳本裡還有帳戶、發票或客戶時會擋下並說明原因（請先把它們移到其他帳本），最後一個個人帳本不能刪除。刪除會同步到其他裝置。
- **總覽的提示可以關閉**：頂端的「資料健康」與「超支」兩條提示可以關掉，之後只有在**情況真的改變**時才會再出現——例如換月、或另一個分類也超支。同一個分類的金額變動不會重複打擾你。

### Changed
- **帳戶頁改用與投資頁一致的視覺**：總資產／總負債／淨值三張卡片改為單一橫條（與投資頁的 KPI 條同款），下方的幣別卡片格改為配置條＋圖例。金額維持完整位數，方便一眼核對「總資產 − 總負債 = 淨值」。
- **刪除動作有了統一的視覺語言**：全 app 的刪除按鈕改用 destructive 樣式。**記帳頁的刪除按鈕過去與編輯按鈕外觀完全相同**，現在有明確區隔。刪除確認的用字統一為「確定刪除」。
- **對話框關閉鈕統一**：15 處關閉鈕改用同一個元件，全部具備 tooltip 與無障礙標籤。iOS 上的觸控區域補足到 44pt——手機「更多」面板的關閉鈕原本沒有可點區塊，只是一個裸的圖示。

### Fixed
- **帳本切換器打不開**：側欄的「總帳」下拉選單被側欄本身蓋住，點了像是沒反應。
- **系統重複建立的「個人帳」會自動合併**：多裝置同步時，每台裝置在第一次啟動（早於首次同步）各自建立自己的預設「個人帳」，造成重複、且每多一台裝置就多一本。現在同步後會自動合併並顯示提示。**只合併系統自動建立且從未編輯過的帳本——你自己建立或改過名稱／設定的帳本永遠不會被自動合併。**
- **投資列表的「編輯持倉」補回價格檢視**：從投資列表開啟的編輯視窗缺少 Yahoo 每日報價檢視與自動／手動切換，從持倉詳情開啟的卻有——同一顆按鈕、兩種功能。現已統一為同一個視窗。
- **淨值增減的漲跌配色**：在「紅漲綠跌（台股）」模式下，總覽的淨值變動維持綠色，與正下方紅色的「投資今日」互相矛盾（兩者描述同一天、且淨值變動幾乎全由投資變動組成）。淨值變動現已歸類為行情數字，跟隨盈虧配色設定。
- **總覽 AI 摘要的排版**：摘要擠在標題列左側、換行破碎、重新產生鈕浮在文字旁，現改為獨立的整列。
- **手機版 KPI 橫條無法左右滑動**：帳戶頁與投資頁的 KPI 橫條在手機上垂直堆疊，設計中的橫向滑動與吸附失效。

## [0.1.0-alpha.61] - 2026-07-14

### Added
- **帳本（公司帳／個人帳／總帳）**：可把公司與個人的收入、支出、帳戶分成不同帳本記錄，側欄切換器可切「個人帳」「公司帳」或「總帳」；一般畫面（淨值、現金流、投資、預算）隨切換器範圍化，FIRE／退休指標與目標永遠只計個人帳、不受切換器影響。每本帳可獨立設定「計入個人淨值」「計入 FIRE 指標」（個人帳預設開、公司帳預設關）。帳戶頁可將帳戶指定到帳本、並管理帳本。跨帳本金流（如發薪水給自己、股東代墊）沿用既有轉帳／代墊機制。
- **開發票與營業稅（公司帳）**：公司帳的記帳新增「開發票」，自動計算 5% 銷項營業稅（含稅／未稅換算）、支援台灣統一發票字軌（2 碼英文＋8 碼數字）與自動流水號，並有客戶主檔（統編、預設收款條件）與自動完成。發票開出後於「未結清」追蹤，對方匯款即結清並記錄收款日。
- **發票報表（公司帳）**：帳齡分析（未到期／逾期 1–30／31–60／61–90／90 天以上）、平均收款週期（DSO）、本期應繳營業稅提醒（依開立當期計算）、雙月 401 銷項稅額彙總表（供報稅參考）。

### Fixed
- **備份還原不再遺失帳本／發票**：修正桌面版（SQLite）備份匯出漏掉帳本的問題，並把發票、客戶納入備份還原全鏈路；舊版備份仍可正常匯入。
- **未結清應收／應付顯示**：修正未指定帳戶的未結清應收／應付在帳本切換（含總帳）時從「未結清」卡片消失的問題。

## [0.1.0-alpha.60] - 2026-07-13

### Added
- **多類別拆分**：一筆消費可拆成多個分類（例如家樂福同時買傢俱與食物）。新增記帳時在分類列按「＋ 分類」進入拆分模式，每個分類各自填金額、上方總額自動加總；交易清單將拆分收合為單列並標示「拆分 N 筆」，點開展開各分類明細；可整組編輯或刪除。分帳（誰欠多少）為下一階段。
- **加入大盤引導**：當投組以時間加權報酬連續 8 季以上落後基準指數、且累積差距超過 5 個百分點時，投資分析頁會出現一則建議式提示（純資訊、可「知道了」或「不再顯示」）。資料不足或領先時不會出現。
- **年度報表列印／匯出 PDF**：年度報表頁新增「列印／匯出 PDF」，經系統列印對話框可另存 PDF；列印版自動隱藏側欄等介面、深色主題轉為淺色墨水，並附報表期間與產生日期。隱私遮罩開啟時按鈕停用。
- **未分類交易批次建議分類**：記帳頁新增建議卡片，依你的歷史記帳與自學字典為未分類交易建議分類，逐筆勾選確認後一次套用；轉帳、代墊與投資連動紀錄不納入，沒把握的不建議。
- **快速記帳（⌘N）商家自動完成**：確認卡的商家欄打字即出現常用商家下拉清單（依使用頻率排序），選取後自動帶入該商家的學習分類；支援鍵盤上下選取。
- **快速記帳範例提示與常用帳戶**：輸入框空白時顯示可點的範例；解析出分類但沒指定帳戶時，自動預設該分類最常用的帳戶。
- **還原前預覽補完**：JSON 備份匯入與同步快照還原現在也會先顯示筆數對照（紅字標示會減少的項目），並須輸入「還原」確認——與本地備份還原一致，三個還原入口行為統一。
- **編輯後自動同步**：開啟同步時，記帳／編輯後約 30 秒自動推送到雲端，不必等切回視窗或手動同步。
- **週期交易分類選擇器**：週期規則的分類改為與記帳一致的結構化選單（原為自由輸入文字），避免打錯字產生幽靈分類；舊規則的自訂文字仍會保留顯示。

### Changed
- **vs 大盤口徑改為時間加權報酬（TWR）**：分析頁「超額報酬」與總覽「vs 0050 累積差距」改以 TWR 計算，不再受加碼／減碼時點影響，介面標示口徑；期間資料不足時退回原「固定權重近似」口徑並照舊註明。
- **快速記帳商家／名稱分離**：不使用 @ 語法時，不再把整句同時填進「名稱」與「商家」。認得的商家（含「50嵐吃晚餐」這類黏在句中的寫法）會自動抽出到商家欄、其餘文字進名稱；認不得的商家留空，由你用自動完成補上。長句仍會交給裝置端 AI 補強解析。

## [0.1.0-alpha.59] - 2026-07-12

### Changed
- **總覽「今日漲跌」改為單欄清單**：上漲與下跌不再左右分成兩欄，改為由上而下一條清單，較好閱讀。
- **持倉篩選列儘量單行**：投資 › 持倉的搜尋、券商、產業、欄位在畫面夠寬時會排在同一列，只有視窗過窄時才換行（先前即使畫面很寬也固定分成兩列）。

### Fixed
- **總覽頂部工具列對齊**：匯率、「更新行情」、「版面」與通知現在固定靠右對齊；先前當本月 AI 財務摘要較長時，這排會被擠到下一行並靠左顯示。
- **分析「上漲最多與下跌最多」的百分比更直觀**：先前百分比顯示的是「佔投組期間總變動的比例」，在投組整體下跌的期間會出現「＋金額卻是 −%」的矛盾。現在改為顯示該標的自身的期間價格報酬，與金額同號（漲的 ＋金額 ＋%、跌的 −金額 −%），並在下方加註說明。

## [0.1.0-alpha.58] - 2026-07-12

### Changed
- **記帳頁工具列收斂**：頂部只保留期間控制與「記一筆」；帳戶／分類篩選收進「篩選」彈出視窗，套用中的條件以可移除的標籤顯示於頁籤下方。期間控制整合為單一控制（左右切換月份，點中間展開預設或月份選擇）。
- **近期動態與固定收支版面優化**：右欄「固定收支」視野由 2 週延長為 30 天並補上每月固定總額，頁首的「未結清」提醒移入右欄並隨頁面固定捲動；交易清單預設顯示最近 3 天、以「顯示更早」逐次多載（取代上一頁／下一頁分頁）；選取超過 3 個月的區間（如 YTD、近 12 個月）時，清單自動以「月」收合，點開才展開當月。

### Added
- **持倉可加「當日損益」欄位**：投資 › 持倉的「欄位」可選擇顯示每檔標的的當日損益金額。

### Fixed
- **投資 KPI 卡片間距**：目前市值等四格數字的文字不再貼齊卡片邊框。
- **投資頁籤多餘捲軸**：持倉／交易紀錄／分析頁籤列不再出現多餘的垂直捲軸。
- **總覽帳戶篩選簡化**：移除總覽的「所有帳戶」篩選（總覽一律彙整所有帳戶），匯率移至與「更新行情」同一列。
- **持倉展開列版面**：點開持倉列顯示的明細改為與設計一致的卡片式排版。

## [0.1.0-alpha.57] - 2026-07-12

### Changed
- **總覽改版（極簡「一眼脈搏」）**：首屏收斂為「淨值＋四格脈搏（投資今日／本月現金流／預算／待辦）＋合併後的『待辦』卡＋今日漲跌」；帳單、信用卡與應收／應付合併進單一「待辦」清單，匯率移至頂部一行小字；資產配置、目標、最近交易、淨值趨勢、30 年預測預設收合，可從「版面」重新開啟。
- **投資持倉表精簡＋可展開**：持倉表預設只留代號／今日／現價／市值／未實現損益，點列展開才顯示均價、股數、券商、成本基礎、股利與當日影響等；四張 KPI 大卡收成一條摘要，配置甜甜圈改為細分佈條。
- **投資分析頁重排**：整頁改用單一全域期間控制（含自訂區間），並依序重排為 01 報酬 › 02 貢獻 › 03 風險 › 04 股利 › 05 集中度，每節標示時間基準；移除 365 天日曆熱點圖（回撤資訊改標在「最大回撤」卡）。

### Added
- **個股詳情「今日」區**：從總覽「今日漲跌」點進個股時，頁面最上方新增「今日」區塊，顯示當日漲跌與對你部位的影響（無行情資料時自動隱藏）。

## [0.1.0-alpha.56] - 2026-07-12

### Changed
- **介面動態全面優化**：抽屜、對話框與各式面板現在關閉時也會平順地滑出／淡出（先前只有開啟有動畫、關閉是瞬間消失）；⌘K 指令列與快速記帳開啟更即時；投資頁的分段切換改為滑動指示。
- **通知提示可滑動關閉**：右下角的通知提示現在可以往側邊滑掉，滑鼠移上去時倒數會暫停，切到其他分頁時也不會在背景默默把時間耗掉。
- **手機版改用底部彈出面板**：新增交易、新增帳戶等表單在手機／觸控裝置上改以底部 sheet 呈現，可直接向下拖曳關閉。
- **隱藏金額不再跳回頁首**：切換隱藏金額（⌘⇧H）時會保留目前的捲動位置。

### Accessibility
- **更完整尊重「減少動態」系統設定**：補上頁面轉場類動畫在系統開啟「減少動態」時一併停用。
- 觸控裝置的點按回饋與 hover 樣式分流、安全區域（瀏海／Home Indicator）間距處理更完善，為 iOS 版本鋪路。

## [0.1.0-alpha.55] - 2026-07-11

### Added
- **用名稱搜尋標的**：新增投資交易時，除了代號，現在也能直接用**中文或英文名稱**搜尋台股與 ETF（例如輸入「台積電」就會出現 2330.TW）。名稱資料取自證交所／櫃買公開資料，離線快取一天。
- **基金用受益憑證代號就找得到**：台灣境內基金現在可用銀行對帳單上顯示的**受益憑證代號**（例如 `T1605Y`）搜尋，不必再記內部基金代號。

### Fixed
- **分類的收入／支出設定不再儲存後跑掉**：在「設定 → 分類」把某分類標記為收入或支出（以及預算結轉設定）並儲存後，記帳頁會正確套用、重開設定也會保留——先前這些設定在每次存檔時都被靜默清掉，等於從未真正生效。
- **找不到基金的問題**：先前基金代號在不同投信之間會互相覆蓋，導致約 3,600 檔基金（含「群益新興金鑽基金」等）在搜尋中消失；現在每檔基金都以唯一的受益憑證代號辨識，全部找得到。基金報價來源偶爾回傳不完整檔案時，也會沿用上一份完整資料，不會讓基金清單與淨值暫時歸零。
- **快速記帳列不再被側邊欄遮住**：視窗較窄時，底部的快速記帳輸入列左緣不會再被側邊欄蓋掉。
- **新增交易抽屜開啟時背景不再捲動**：開啟「新增交易」後，背景頁面會正確鎖住，側邊欄不會在捲動後跑掉、左側變空白。
- **週期規則首次執行日期時區修正**：台灣清晨（午夜到早上 8 點）新增週期性收支或定期定額時，首次執行日期不會再早一天。

### Security
- **裝置識別碼改用更安全的隨機來源**：在缺少加密級亂數的環境下，裝置 ID 的備援產生方式改用密碼學安全隨機源，並修正了 CI 權限與相依套件的安全性告警。

## [0.1.0-alpha.54] - 2026-07-10

### Security
- **配對更安全**：新增裝置時，vault 金鑰不再以短碼加密後上傳中繼站，改為透過 ECDH 金鑰交換直接包裝給新裝置的公鑰——中繼站永遠讀不到你的財務金鑰。配對時舊裝置會先顯示新裝置名稱與金鑰指紋供你確認。
- **裝置撤銷真的生效**：每台裝置現在持有自己的憑證，「撤銷裝置」後該裝置會立即失去對同步資料的存取權（先前所有裝置共用同一把鑰匙，撤銷只是形式）。
- **金鑰加密儲存**：vault 金鑰與裝置金鑰改由加密的 Stronghold 保管；「重置此裝置」會真正清除所有機密。
- 中繼站改為每帳號獨立序號並加入速率限制，避免不同帳號互相影響。

### Added
- **自訂資產更好找**：手動定價資產（未上市基金、不動產、收藏品等）的建立入口更清楚；當某筆自訂資產的價格超過 90 天未更新（或尚未定價）時，會在持倉上顯示「價格過期」／「未定價」標記。

### Changed
- **投資損益配色**：個股明細與投資分析頁的損益、報酬率顏色，現在會跟隨你的「盈虧配色」設定（例如台股使用者選紅漲綠跌時，這些數字也會一致地紅漲綠跌）。
- **無障礙提升**：對話框／彈窗現在會把鍵盤焦點鎖在視窗內、可用 Escape 關閉、並讓螢幕閱讀器正確辨識；圖示按鈕加上了無障礙標籤。
- 彈窗遮罩統一改用帶品牌色調的深色，不再是純黑。

### Fixed
- **編輯投資交易不再覆寫手續費**：開啟既有的台股買賣紀錄修改時，不會再把你原本記錄（或匯入）的手續費覆蓋成系統估算值。
- **缺少匯率不再被當成 0 元**：某外幣當下沒有匯率時，先前會被靜默算成 0 導致淨值／配息／報稅金額偏低；現在會排除並在年度報稅頁標示「有 N 筆配息因缺少匯率未計入」。
- **時區日期修正**：台灣清晨（午夜到早上 8 點）時，總覽的本月現金流與預算欄不會再顯示成上個月。
- **帳戶重新計算**：從帳戶頁執行「重新計算」後，持倉數字會正確更新。
- CSV 匯入失敗時會顯示錯誤提示（先前無聲無息）。
- 修正週期性交易在多裝置同步時可能重複或不一致的問題。

## [0.1.0-alpha.53] - 2026-07-05

### Changed
- **本月摘要更融入版面**：裝置端 AI 的「本月摘要」從頁面底部的卡片移到問候語（早安／午安／晚安）正下方，改為低調的一段文字，不再像獨立卡片般突兀。摘要中的**粗體**重點（如金額、儲蓄率）現在會正確顯示為粗體，不再出現 `**` 符號。
- **更新通知更精簡**：「有新版本可下載」的提示預設只顯示一句話與「立即更新」，完整的更新內容收合在「更新內容」裡，點開才展開；展開的內容會正確排版（粗體、標題、清單），不再顯示原始的 Markdown 符號。

## [0.1.0-alpha.52] - 2026-07-03

### Added
- **年度報稅明細**：年度報表可展開每個年度的逐檔明細（已實現損益、股利），並分「境內 / 海外」小計，另可匯出 CSV 供報稅使用。

### Changed
- **通知中心移至總覽**：通知鈴鐺從側邊欄移到總覽頁右上角，點開向下展開；手機版也首次能查看通知。
- **卡片標題中文化**：「N 個進行中目標」「今日漲跌」等原本英文的卡片標題改為中文。
- **手機版總覽更精簡**：頁首控制項在手機上收合為更緊湊的排列；淨值時間範圍切換條加上可捲動的視覺提示。
- **圖表語意調整**：30 年預測與 FIRE 的「保守」情境改用琥珀色（紅色保留給實際虧損）；FIRE 圖表會標出「預估資產耗盡」的年齡；彈出視窗的遮罩改用帶品牌色的深色，不再是純黑。

### Fixed
- **隱藏金額失效修正**：開啟「隱藏金額」後，目標、FIRE 計算機、設定分類頁的金額過去仍會明文顯示；現在一律正確遮罩。
- **首頁數字可信度**：資料很少時，首頁淨值變化不再顯示無意義的巨大百分比（如 +570%）；分析頁的年化 XIRR 若紀錄不足一年會加註「僅供參考」；首頁大數字不再被截斷成「TWD 86…」。
- **設定分類「已消費」修正**：設定 → 分類的「已消費」過去固定顯示 NT$0，現在顯示當月實際花費，與記帳頁一致。
- **投資輸入小數精度**：新增投資交易時，價格與股數現在可輸入到小數點後 5 位，不再被無聲截斷。
- **股息再投入（DRIP）**：現在會自動帶入再投入金額，且金額與「股數 × 價格」的驗證放寬到券商捨入等級，不再因差幾毫而無法記錄。
- **macOS 視窗拖曳**：sidebar 頂部（logo 與留白）現在也能拖曳整個視窗。
- **資產配置現金同色**：資產配置圓餅的「現金」與上方 KPI 卡的「現金 / 存款」現在同色（藍），不再因排序而變色。

## [0.1.0-alpha.51] - 2026-07-02

### Changed
- **快速記帳分類篩選**：快速記帳（⌘N）的分類選單現在會依「支出 / 收入」只顯示對應分類（與記帳頁一致），不再一律列出全部分類。
- **投資交易摘要卡片連動篩選**：投資「交易紀錄」上方的總買入 / 總賣出 / 總股利 / 筆數，現在會隨券商、類型、搜尋等篩選條件即時連動，數字與下方清單一致。

### Fixed
- **修復匯入持倉影響券商餘額**：編輯「匯入現有持倉」的期初部位時，過去會誤記一筆交割現金流、改變券商帳戶餘額；現在期初部位維持不影響現金，並會在載入時自動修復先前受影響的餘額。（連帶：總買入不再把匯入部位計為買入。）
- **側邊欄與通知顯示修正（macOS）**：側邊欄的 logo 不再被視窗左上角的紅綠燈按鈕擠壓；點開通知面板時內容不再被邊界裁切。
- **更新內容說明**：應用程式內「檢查更新」的「更新內容」現在會顯示該版本完整的更新說明，先前只會顯示版本號。

## [0.1.0-alpha.50] - 2026-07-02

### Added
- **通知中心**：側邊欄新增通知中心（鈴鐺），列出即將到期的信用卡繳款提醒；可逐筆或「全部標為已讀」，macOS Dock 上的紅點只計未讀提醒，讀完即消失。

### Changed
- **可搜尋台灣境內基金**：新增投資部位、搜尋代號時，現在可用基金名稱或代號找到台灣境內共同基金（SITCA），選取後以 `SITCA:<代號>` 記錄。
- **ETF 顯示中文名稱**：搜尋台股 ETF（如 `00878`）現在顯示中文名稱（如「國泰永續高股息」），不再只顯示 Yahoo 的英文名。

### Fixed
- **修復跨裝置同步崩潰**：多裝置同步時，週期交易重複會觸發 `recurring_occurrence_key` UNIQUE 衝突（code 2067）導致整批同步中止；現在會自動去重，兩台裝置收斂為同一筆。
- **修復 macOS 視窗無法拖曳**：補上 `core:window:allow-start-dragging` 權限，透明標題列現在可正常拖曳整個視窗。

## [0.1.0-alpha.45] - 2026-06-26

### Added
- **安全性與隱私強化**：將 `apiSecret` 等同步帳號憑證從普通的本地端儲存 (localStorage) 無痛移轉至更高安全級別的 `@tauri-apps/plugin-stronghold` 加密儲存，進一步保障您的財務資料安全。舊版憑證在啟動時會自動完成安全移轉。

### Changed
- **券商手續費設定介面**：設定頁面的券商手續費改為直接勾選「適用與否」，當不適用時不再需要輸入折數，使用上更直覺。
- **股票搜尋顯示中文名稱**：在新增投資部位時搜尋股票代碼（如台股），現在搜尋清單會正確為您顯示台灣公司的中文名稱，而非 Yahoo Finance 預設的英文名稱。

### Fixed
- **信用卡繳款提醒修正**：修復了首頁信用卡對帳模組。現在系統會精準判斷帳單結帳與繳費狀態，已經完成繳款的信用卡紀錄不再會反覆出現於首頁提醒中。
- **修正後端資訊暴露與設定**：修正了同步伺服器 (Worker) 潛在的錯誤訊息暴露，以及修正了 GitHub Actions 權限。

## [0.1.0-alpha.43] - 2026-06-25

### Added
- **台灣共同基金自動淨值**：新增持倉時，台灣境內基金可用 `SITCA:<基金代號>` 當代號並把類型選為「共同基金」，系統會每日自動向投信投顧公會（SITCA）的公開淨值更新，淨值用原幣別計價、再依匯率換算，不必再手動輸入。
- **投資分析新增「國家 / 地區」分布**：依各持股的上市市場自動歸類地理曝險（.TW / .TWO → 台灣、美股 → 美國、.T → 日本、.HK → 香港、.SS / .SZ → 中國、.L → 英國 …），讓你一眼看出投資組合有多少押在哪些國家；無法判斷的會明確歸入「未知地區」而非略過。
- **ETF / 基金不再顯示「未知分類」**：行業分布把 ETF 與基金歸入「ETF / 基金」分類桶，並可手動標記它的主要行業 / 地區（手動標記優先於自動結果）。

### Changed
- **台股顯示正式中文名稱**：台股（.TW / .TWO）若先前只抓到英文名，執行「回補分類」時會重新向證交所 / 櫃買中心的公開資料抓取正式中文名稱並更新顯示（美股等沒有中文來源的標的維持英文）。

### Fixed
- **「回補分類」不再覆蓋你手動輸入的分類**：你手動設定過的資產類型 / 產業 / 行業會被鎖定，之後再執行「回補分類」只會補上缺漏、不會把你的設定洗掉。

## [0.1.0-alpha.39] - 2026-06-19

### Added
- **信用卡繳款改為轉帳，並可填回饋 / 折抵**：對帳頁的「繳款」改為可選擇付款帳戶並實際轉帳扣款，銀行餘額與卡片未繳會一併更新；另可填寫帳單折抵 / 現金回饋，少付的部分會記為卡片的回饋入帳。
- **延後入帳（信用卡）**：新增信用卡支出時可設定「當下入帳」或「延後到某日」；延後的消費會歸入「入帳日」所屬的帳單週期（仍立即計為負債、不影響餘額，消費日期也不變），適合結帳日前後或延遲入帳的刷卡。
- **各券商手續費折扣**：「設定 → 交易成本」可為每個券商帳戶設定手續費折數（例：6 折），自動試算買賣時套用於券商手續費；證券交易稅為法定費率，不打折。交易輸入時會顯示套用的折數。

### Changed
- **對帳時可直接編輯交易**：對帳頁每筆交易新增編輯入口，點擊後會在現金流開啟該筆明細，可修改金額、海外手續費、折扣等資訊。
- **帳戶餘額欄位更名為「期初餘額」**：新增 / 編輯帳戶時的餘額欄位由「當前餘額」更名為「期初餘額」，並加註說明（目前餘額 = 期初餘額 + 已結算交易；要直接修正目前餘額請用帳戶列的「調整餘額」），避免把起始餘額誤當成目前餘額填寫。

## [0.1.0-alpha.38] - 2026-06-19

### Added
- **北極星指標與底氣數據**：總覽 Hero 可在淨值、儲蓄率、保障倍數（緊急預備金可支應幾個月）、生活續航（現有現金可撐多久）與 FIRE 進度之間切換主指標，並記住偏好。
- **未來淨值複利推估**：總覽新增可隱藏的淨值推估卡，以近 3 個月平均淨流入為基礎畫出 30 年複利推估區間（含保守 / 樂觀帶與可調整的報酬率）。
- **台股交易成本自動試算**：新增交易時可自動預填台股券商手續費（0.1425%、最低 NT$20）與證券交易稅（股票 0.3% / ETF 0.1%）；可在「設定 → 交易成本」開關與調整費率，僅適用 .TW / .TWO 標的且可手動覆寫。
- **目標達成進度（超前 / 落後）**：自訂目標可設定目標日期，並依線性進度顯示「超前 / 落後 / 準時」狀態。

### Changed
- **基準對比改為白話累積落後 / 領先**：投資分析以「期間累積落後 / 領先 0050 X%」說明與大盤的差距，並可選為總覽主指標。
- **修正總覽淨值對比標籤與圖表端點**：Hero 漲跌數字旁的文字先前仍寫死「較上月」，現改為依所選區間顯示（近 1 個月 / 近 3 個月 / 今年以來 …）；並讓淨值圖表結尾的當日數值與上方大字淨值一致。

### Fixed
- **信用卡餘額改以「未繳 / 溢繳」呈現**：信用卡帳戶卡片改用未繳（負債）、溢繳（預付）、已結清的語意顯示，與對帳頁口徑一致；並修正新增帳戶時誤導的餘額提示。
- **修正刪除帳戶未連動造成的孤兒交易**：以代墊方式關聯到應收 / 應付的帳戶不再能被直接刪除而留下孤兒交易；資料健檢會列出仍連結到已刪除帳戶、持續影響餘額的交易，提醒結清或刪除。
- **記帳近期動態可翻頁**：交易明細補上「上一頁 / 下一頁」分頁，當月交易較多時也能看到完整紀錄（先前僅顯示前 50 筆）。

## [0.1.0-alpha.36] - 2026-06-16

### Added
- **載入與錯誤狀態**：總覽、投資、記帳、帳戶、分類、目標、FIRE 計算機、持倉與商家詳情、對帳、設定、交易紀錄等主要頁面，在資料載入時改用骨架畫面取代空白或歸零畫面；讀取失敗時會顯示明確錯誤訊息與「重新整理」按鈕。新增全域錯誤邊界，避免單一頁面發生例外時整個應用白屏。
- **投資分析揭露計算方法**：分析頁新增固定持股權重近似法的說明註記，標示報酬、超額報酬與風險指標（波動、Sharpe、Sortino、最大回撤）的計算基礎與限制。

### Changed
- **修正部分介面殘留英文字串**：記帳、投資、總覽等頁面的「Recent activity」「Ticker」「Alpha」「Gainers」「Losers」「Asset」等字樣改為繁體中文。

## [0.1.0-alpha.35] - 2026-06-15

### Added
- **總覽淨值區間更完整**：淨值趨勢新增 `1D`、`5Y`、`All` 區間（完整為 `1D / 1W / 1M / 3M / YTD / 1Y / 5Y / All`），並改用每日粒度計算，短區間也能畫出真實曲線。

### Changed
- **淨值旁的漲跌數字會跟著區間變動**：總覽 Hero 的 `+/-` 金額與百分比改為反映所選日期區間（區間起點 → 現在），不再固定顯示上月對比。
- **同步重設拆成兩個功能**：Connect 同步新增「解除同步」（清除同步設定但保留本機資料）與「完整重設資料」（解除同步並清除資料，需輸入 `delete` 確認）；移除示範區重複的「清空所有資料」。
- **投資分析期間「Total」更名為「All」**。
- **暫時隱藏定期定額功能**：投資頁與總覽移除定期定額分頁與提醒，待使用方式確定後再開放（資料與元件保留）。

### Fixed
- **修正 Connect 同步推送失敗（500）**：補上遠端資料庫缺少的 `relay_sequence` migration，同步可正常推送；Worker 也改為回報真正的錯誤原因，方便診斷。
- **修正設定中商家改名後列表沒更新**：改名後會即時反映在商家清單，並避免重複觸發。

---

## [0.1.0-alpha.34] - 2026-06-14

### Changed
- **投資分析的上漲 / 下跌排行改用成本基準**：Analytics 的「上漲最多與下跌最多」改為使用使用者自己的持股數、移動平均成本與最新市值計算未實現損益，百分比也改為該持倉相對成本的報酬率，和持倉頁的損益口徑一致。
- **持倉篩選列版面調整**：持倉頁的「所有券商」「所有產業」「欄位」控制項改為同高、可換行且不裁字；桌面寬版維持同列，手機版自然換行。

### Fixed
- **修正投資分析長金額造成跑版**：報酬排行的金額欄位加入收縮與省略處理，避免像 `+TWD 1,005,928` 這類長數字把卡片撐開。
- **修正持倉篩選的 all-state 顏色不一致**：持倉頁的「所有券商」不再顯示成 placeholder muted 色，和「所有產業」保持一致。

---

## [0.1.0-alpha.33] - 2026-06-14

### Added
- **桌面側欄可收合**：桌面版側欄新增收合 / 展開控制，會保留搜尋、快速記帳、主要導覽與隱私模式入口，並記住使用者偏好。
- **投資分析新增期間與區段導覽**：分析頁補上 `1M`、`5Y`、`Total` 期間，並加入報酬、風險、配置與股利的頁內導覽，長頁閱讀更順。
- **投資配置可依產業深入查看**：配置分析改以產業呈現，支援本地化產業名稱與從分析頁帶入產業篩選，方便追蹤單一產業曝險。

### Changed
- **新手導覽入口移到需要時顯示**：完成導覽後側欄不再常駐顯示入口；設定中新增「重新開始新手導覽」。
- **更新公開截圖素材**：新增一批最新介面的產品截圖，涵蓋總覽、現金流、投資分析、持股詳情、FIRE 試算與帳戶頁。

### Fixed
- **修正投資分析短期間可能整頁空白**：頁面是否可用改看完整價格歷史，不再因切到短期間或年初 YTD 資料不足而隱藏整個分析頁。
- **修正匯率設定小數顯示過長**：手動匯率顯示統一收斂到最多 6 位小數，避免輸入框出現浮點尾數。

---

## [0.1.0-alpha.32] - 2026-06-14

### Added
- **本地自動備份與手動備份**：設定中新增本地備份能力，Northstar 會在本機保留可還原的備份紀錄，也可手動建立備份，降低資料誤刪或測試流程中的風險。

### Changed
- **投資分析分頁改為 Direction A editorial layout**：投資分析頁改成更有層次的 editorial 儀表板版面，讓核心投資指標、分析區塊與視覺重點更清楚。
- **設計系統與 app icon 更新**：補上設計系統文件、更新 token palette，並重新整理 Northstar app icon 與各尺寸圖示。
- **公開 source build / release 資產流程調整**：銀行與券商 logo 改為 release-only private asset injection，公開 source build 在沒有 private assets 或官方同步 endpoint 時也能正常建置。

### Fixed
- **修正示範資料退出時正式資料可能被覆蓋的風險**：示範模式切換改用 IndexedDB 保存正式資料快照，避免 localStorage 容量限制或序列化問題造成還原不完整。
- **修正 macOS dark mode Sidebar 被 native glass 洗成灰色**：保留 macOS 原生 vibrancy，但在深色模式下加強 Sidebar 的 app theme tint，避免主內容是暗色、Sidebar 卻偏灰的視覺失配。

---

## [0.1.0-alpha.31] - 2026-06-14

### Changed
- **Light 主題配色全面調整**：畫布換成冷調 off-white，卡片改為純白（兩層分開，讓卡片真正「浮起來」）。品牌綠調亮（`#8ed34e`），accent 文字改為深綠（`#173708`），與深色模式的品牌色邏輯一致。正色／負色在白底上加深以符合 WCAG AA 對比度，圖表配色同步對齊。

---

## [0.1.0-alpha.30] - 2026-06-13

### Added
- **快速記帳（⌘N）的自然語言解析全面升級**：先前是「抓第一個數字當金額、其餘當商家」的單一規則，常常解錯（例如 `7-11 50` 會把 `7` 當金額）。現在改為逐欄位解析——金額、日期、帳戶、標的、分類各自獨立抽取並附信心度，邊打字邊即時預覽（會跳出金額／日期／帳戶／分類的小標籤）。支援中文數字與口語寫法（`一百二`→120、`5萬`→50000、`1.2k`→1200）、相對日期（`昨天`、`上週五`）、帳戶模糊比對（打「富邦」也能對到「富邦證券」），中英文走同一套流程。即使只解析出部分欄位也會先帶入，不再「全有或全無」。
- **裝置端 AI 增強（Apple Foundation Models）**：在支援的 Apple 裝置上（macOS / iOS 26+、Apple Silicon 並開啟 Apple Intelligence），當規則解析信心不足時，會自動交給**裝置端**模型補強，完全在本機運算、不連網、不外送任何資料。解析來源為 AI 時會在確認卡標示「AI」，輸入區也會顯示「裝置端 AI · 可用／不可用」提示。不支援的裝置（Windows／Linux／較舊 macOS）自動落回規則解析，功能不受影響。
- **自動分類會自我學習**：新增使用者字典（UserLexicon），會從你的歷史交易與每次的修正回饋逐步學習商家／關鍵字對應的分類，讓第一次出現、或歷史記憶沒命中的商家也能猜得更準。

### Changed
- **商家／分類詳情的「消費模式」改顯示金額、星期標示更直覺**：星期分佈長條圖由原本統計「筆數」改為統計「消費金額」（hover 顯示金額），高峰判定也改用金額；標題的星期寫法由「二曜日」更正為「星期二」。
- **商家詳情「自動分類」面板文案更清楚**：明確說明這條規則「下次用快速記帳輸入此商家時會自動套用」，並把推導出的規則以等寬字凸顯；交易太少時的提示也改寫得更白話。

---

## [0.1.0-alpha.29] - 2026-06-13

### Added
- **內建台灣銀行／券商 LOGO，且預設顯示**：先前帳戶 LOGO 需在設定裡手動開啟、且還沒有內建任何圖檔。現在已內建一批常見品牌的 LOGO——富邦、國泰世華、玉山、台新、Richart、永豐（含大戶）、第一／iLEO、將來銀行、LINE Bank、凱基、Firstrade、Crypto.com——並改為**預設顯示**，完全離線、不對外發送任何請求。會依帳戶名稱（如「玉山」「國泰」）自動對應，也可在帳戶編輯裡手動指定品牌；尚未內建的品牌則顯示你選的圖示。設定中原本的「帳戶銀行 LOGO」開關已移除（不再需要）。

### Fixed
- **投資分析的股利統計會漏算已賣出標的領過的股利**：先前殖利率與近一年股利（TTM）只看「目前還持有」的標的，若一檔股票配息後又被你全部賣掉，那段時間領到的現金股利就不會被計入。現在股利統計會涵蓋所有曾持有的標的（含已出清、零股數），數字更完整。順帶修正某些情況下持倉現值的計價幣別回退，避免換算成主幣時取到錯誤幣別。

---

## [0.1.0-alpha.28] - 2026-06-12

### Fixed
- **修正多裝置同步會吃掉手動持股的股數**：若一檔股票一開始用「手動新增」（例如 400 股），之後又記了買進交易（例如 +20 股），同步到另一台裝置時只會剩下交易的 20 股、手動的 400 股不見了。原因是手動持股的「期初部位」紀錄在同步時可能落後於後續交易，導致另一台只用交易重算出錯誤股數。現在期初股數會固定保存在持股本身（隨持股一起同步），就算期初紀錄稍後才到也能正確還原；同時補上週期投資（定期定額）紀錄的同步驗證，避免一筆未支援的紀錄中斷整批同步。
- **銀行／券商 LOGO 改為離線內建**：帳戶 LOGO 先前向第三方服務（Clearbit）請求，但該服務已停止運作，導致 LOGO 一律無法載入，且改用網路 favicon 的畫質參差不齊。現改為顯示<strong>內建</strong>的銀行 LOGO——完全離線、不對外發送任何請求、畫質一致。LOGO 也會套用到記帳頁等的帳戶下拉選單，並可在帳戶編輯裡手動指定品牌。內建 LOGO 會陸續補齊；尚未內建的品牌會顯示你選的圖示。
- **「清空所有資料」加上防呆**：此按鈕會永久刪除本機所有正式資料（非示範資料）。現在會明確警告、提醒先匯出備份，並需輸入 `delete` 才能執行，避免誤觸。

---

## [0.1.0-alpha.27] - 2026-06-07

### Added
- **證券交易匯入精靈（欄位對應 + 範本）**：投資頁的「匯入 CSV」改為三步驟精靈（上傳 → 欄位對應 → 預覽匯入）。可把任意 CSV 的欄位自由對應到日期／類別／代號／數量／價格／手續費等，並支援中文標題與券商格式（如 Firstrade 的「日期／交易類別／代號」、`買進`／`賣出`、`MM/DD/YYYY` 日期）。會自動偵測欄位與交易類別，未知的類別可逐一指定或略過。對應方式可**存成範本**（依券商命名），下次匯入直接套用、更新或刪除。匯入前先選定投資帳戶，交易會以該帳戶幣別交割；無法匯入的列會列出原因。
- **獨立「匯出」分頁**：設定新增「匯出」分頁，可選格式（CSV／JSON）、時間範圍（本月／上月／今年至今／去年／全部／自訂）、帳戶範圍與包含欄位（內部轉帳／投資交易／備註／FX 快照），並即時顯示預計匯出筆數。CSV 依篩選輸出，JSON 則為整份資料庫的完整備份。
- **現金流圖表可切換時間維度（日／週／月／年）**：記帳頁的現金流圖由原本的「累積淨額」面積線圖改為**收入／支出對照長條圖**（收入向上、支出向下）外加淨額折線，並可用日（當月）、週（近 12 週）、月（近 12 個月）、年（近 6 年）切換群組單位。

### Changed
- **數字字體改用 Geist（更易讀）**：餘額、金額、百分比等數字改用比例字體 Geist + tabular 對齊（取代原本會 fallback 成系統等寬字的設定），閱讀體驗接近 Wise；標籤／代號／驗證碼維持等寬風格。

### Fixed
- **修正備份「匯入」沒有反應**：先前在桌面版按下「匯入備份」選檔後毫無動靜也無錯誤訊息——原因是確認對話框在 Tauri 視窗中無效，導致整個流程被中止。現改為選檔後顯示確認橫幅，按下「確定匯入」才覆蓋資料，並會帶出實際錯誤原因。
- **修正「刪除分類」按不動**：同一個確認對話框問題也讓刪除分類無效，現改為點兩下確認。
- **轉帳金額不再顯示 +/− 號**：帳戶間轉帳屬於中性資金移動，列表中不再標示正負號（先前合併轉帳列會誤加「−」）。

---

## [0.1.0-alpha.26] - 2026-06-07

### Added
- **應收／應付支援「代墊」**：記錄應收／應付時可指定一個「代墊帳戶」——例如你先用現金幫別人付款。建立當下就會從代墊帳戶扣款，等對方還款時再結清；整筆代墊視為資金過路、淨額為零，不會被算進收入或支出。
- **結清時才選收款／付款帳戶**：建立應收／應付時不必先填對方會還到哪個帳戶（通常當下還不知道）。按「✓ 結清」時才跳出對話框，讓你選擇款項實際收到／付出的帳戶。
- **週期交易的編輯範圍**：編輯由週期規則自動產生的記帳時，會詢問要套用到「只改此次」「此次與未來」或「全部紀錄（過去＋現在＋未來）」，就像行事曆改重複行程一樣。

### Changed
- **記帳預設為單筆交易**：新增交易時「週期交易」一律預設為「單次（不重複）」，不再殘留上一次選過的週期設定。
- **交易明細的轉帳合併為一列**：同一筆轉帳的「轉出 / 轉入」不再拆成兩列，改以「來源帳戶 → 目標帳戶」單列呈現（跨幣顯示匯率與雙幣金額），明細更精簡。
- **分類／商家詳情版面**：重新平衡左右欄（左側為視覺圖表、右側為數字與排行），消除欄位高低落差造成的大塊留白。

### Improved
- **詳情圖表可互動**：月支出與星期分佈長條圖，滑鼠移上（桌機）或點按（手機）會顯示該欄的金額／筆數；數值為 0 的月份或星期不再顯示殘留的長條。

---

## [0.1.0-alpha.24] - 2026-06-05

### Added
- **投資分析分頁**：投資頁新增「投資分析」分頁，提供投資組合的報酬與走勢分析。
- **大盤基準比較**：分析走勢可對照 0050.TW 作為基準線（自動補抓歷史資料）。
- **漲跌幅排行（Top Movers）**：以當日漲幅／跌幅分組呈現持股表現，週末沿用最後交易日報價避免顯示異常。

### Changed
- 手動持股以代號（ticker）定價，使分析與現有報酬計算一致；分析在資料天數不足時會先隱藏，待累積足夠歷史再顯示。

---

## [0.1.0-alpha.23] - 2026-06-04

### Added
- **快速記帳「記帳 / 投資」切換**：快速記帳輸入列新增類型切換鈕。選「投資」即可直接解析「2330.TW 5股 @1042」「賣 AAPL 10 @180」這類輸入，不必再以「買 / 賣」開頭，解析準確度大幅提升。

### Changed
- **手動持股改用「期初部位」模型**：新增手動持股時會建立一筆對應的期初成本紀錄（不影響現金餘額），讓股數、平均成本、報酬率（XIRR）、淨值走勢與損益全部從交易紀錄一致計算。日後針對同一檔記錄買賣，平均成本會正確加權；手動持股也能無縫轉為逐筆交易追蹤。

### Fixed
- 修正總覽「淨值走勢圖」在持有手動／匯入持股時出現「先平後暴衝」與虛假數千 % 漲幅徽章的問題——持股成本現在自取得日起就計入歷史。
- 修正手動持股在記錄後續買入後，平均成本與損益（已實現／未實現）被凍結、計算錯誤的問題。
- 修正手動持股被排除在投資組合年化報酬（XIRR）之外、導致報酬率顯示「–」的問題。

### Notes
- **升級提醒**：既有「手動持股 + 後續買入」的平均成本會由先前的快照值修正為正確的加權平均值，因此部分數字可能變動（這是修正既有錯誤，非資料異常）。

---

## [0.1.0-alpha.22] - 2026-06-03

### Changed
- **全面導入 COSS UI**：Dashboard、記帳、投資、帳戶、設定、FIRE／目標與 App 外框全面遷移到 COSS UI（Base UI／Tailwind v4）設計系統，並移除舊有 `ns-*` 工具類別。

### Improved
- **25 項 UI／UX 優化批次**：涵蓋區段選取、金額輸入、現金流分頁導覽、對比與精度等多處細節調整。

### Internal
- 降低 GitHub Actions 發布成本（支援 self-hosted macOS runner 與本機發布腳本）。

---

## [0.1.0-alpha.21] - 2026-06-03

### Added
- **可搜尋帳戶篩選**：總覽與記帳頁的「所有帳戶」下拉改為可搜尋、依類型分組、帶圖示的選單（共用 `AccountFilter`），帳戶多時也好找。
- **Phosphor 圖示系統**：帳戶與分類圖示改用內建的 Phosphor 圖示選擇器（含搜尋與分類），取代 emoji；舊有 emoji 資料仍可正常顯示。

### Improved
- **背景自動同步收尾**：套用遠端變更後自動刷新畫面；設定頁顯示「同步中…」與「上次同步：X 秒前」即時狀態。
- **Recovery Kit 安全守門**：未備份並確認備援碼前，雲端同步不會啟動（自動同步靜默等待、手動同步停用並提示），避免在未備份金鑰下啟用雲端功能。

### Fixed
- 移除未使用的 `emoji-picker-react` 依賴；散落的裝飾性符號改用 Phosphor 圖示。

---

## [0.1.0-alpha.20] - 2026-06-03

### Added
- **投資配股與減資**：投資交易支援記錄「股票股利（配股）」與「現金減資」，正確調整股數與平均成本。

### Improved
- **對帳金額語意**：信用卡對帳區分「帳單總刷卡額」與「淨額」，金額呈現更清楚。
- **FIRE 一致性**：報酬基準統一、提供 3 種情境的達成機率，並把所得通膨納入試算。

---

## [0.1.0-alpha.19] - 2026-06-03

### Added
- **目標 HeroCard 可切換**：上方目標卡可自選要呈現哪一個進行中的目標；FIRE 顯示退休預估曲線，一般目標改顯示「目前淨值 / 目標金額」進度。預估趨勢圖補上 X／Y 軸刻度、提示與目標參考線。
- **信用卡對帳分期**：對帳頁依結帳日把交易切成各期帳單，預設只展開本期、歷史各期可折疊；已繳款期別標示並與本期分離，上一期繳完即可獨立核對。
- **應收／應付提醒**：總覽與記帳頁新增未結清「應收 / 應付帳款」提醒 widget。
- **投資定期定額 / 定期定股**：投資頁新增「定期定額」分頁，可設定週期、金額或股數與扣款（交割）帳戶；到期時於總覽與投資頁提醒備妥交割款，一鍵記錄即建立買入並扣交割款。

### Improved
- **目標編修**：由鉛筆進入會帶入現有設定值，按鈕改為「儲存變更」並原地更新，不再誤建新目標。
- **帳戶列表**：移除每列占比長條，版面更乾淨、餘額對齊。

---

## [0.1.0-alpha.18] - 2026-06-02

### Fixed
- 端到端自動更新驗證版本（確認 alpha.17 起的 in-app updater 能正確偵測並安裝新版）。

---

## [0.1.0-alpha.17] - 2026-06-02

### Fixed
- **自動更新**：修正 in-app updater 一直顯示「已是最新版本」的問題。更新來源改為公開的 `northstar-releases` repo（原始碼維持 private），讓 updater 能正常抓到新版本與簽章。此版起安裝的版本之後都能自動更新。

---

## [0.1.0-alpha.16] - 2026-06-02

### Added
- **交易紀錄篩選**：投資的「交易紀錄」分頁新增「交易種類」「券商」兩組多選下拉篩選，並把轉帳到券商的入金紀錄一併納入列表，每筆顯示幣值與券商。
- **帳戶 → 記帳直達**：在帳戶頁點擊帳戶名稱，會跳到記帳頁並自動篩選該帳戶的交易。

### Improved
- **同步衝突中心**：同 revision 的差異改為依編輯時間自動裁決（新的覆蓋舊的），只有真正同時間的衝突才會列出；衝突卡片改為可讀（實體、標題、哪一版較新、逐欄位差異），並新增「全部保留本機／全部採用遠端」批次處理。
- **持倉 KPI 數字**：依介面語言以萬/億或 K/M 精簡顯示，數字不再被裁切。
- **市值排行**：以換算後的本位幣（TWD）排序，USD 部位不再被當小數。
- **總覽淨值卡片**：尚無趨勢時收合成精簡提示，不再佔用大片空白；資產配置圖例在窄螢幕不再溢位。
- **持股詳情**：趨勢圖與 Your Position 卡片等高對齊；無交易紀錄時，持倉天數改從新增持倉日期起算。

### Fixed
- 投資「交易紀錄」分頁移除重複的新增按鈕（保留 Header 的 Buy / Sell）。
- 持倉配置圓餅圖的提示文字在暗色佈景下不再被吃掉；過長的公司名稱會截斷並提供完整名稱提示。

---

## [0.1.0-alpha.5] - 2026-05-29

### Added
- **互動式分類 Donut 圖表**：記帳頁面右側新增可點擊的圓餅圖，直觀呈現各分類支出佔比。點擊任一扇區即可快速篩選該分類的交易明細。
- **分類篩選**：記帳頁面新增分類下拉篩選器，可單獨或搭配帳戶篩選使用，讓你快速聚焦特定類別的支出。
- **交易詳情面板**：點擊任一交易後會滑入詳情側面板，完整呈現金額、分類、商家、帳戶、日期等資訊。從面板可直接編輯或刪除交易。
- **分類管理 Drawer 正式啟用**：記帳頁面的「分類管理」按鈕現在會正確開啟分類管理側欄，可直接新增、重命名、刪除分類與子分類。
- **分類頁面升級**：分類總覽頁面的圓餅圖現在可點擊篩選，分類圖示改用 emoji 呈現，並可直接開啟分類管理。

### Improved
- **圖表互動連動**：Donut 圖表、分類清單、篩選下拉選單三者雙向同步——點擊任一處都會同步更新其他元件的狀態。
- **多國語系基礎設施 (i18n)**：側邊欄導覽與設定頁面已套用 react-i18next，支援繁體中文和英文切換。
- **設定頁面重構**：全新雙欄式設定介面，包含分類、商家、匯率、一般設定四大分頁。

---

## [0.1.0] - 2026-05-28

### Added
- **Global Search**: Instantly find specific transactions, accounts, and holdings across your entire financial life with a new unified search bar.
- **Cash Flow Entry Drawer**: Seamlessly record income and expenses without losing your place. The new slide-out drawer makes data entry fast and frictionless.
- **Holdings Detail View**: Dive deeper into your investments. You can now tap into individual holdings to see their performance and transaction history in one place.
- **FIRE Calculator**: Plan your financial independence. A new built-in calculator helps you visualize your trajectory to retirement based on your current savings rate and net worth.

### Improved
- **Modernized Interface**: We've completely overhauled the visual design to provide a cleaner, more accessible, and premium feel across the entire application.
- **App Shell Experience**: Navigation is now smoother and more intuitive, keeping your most important financial tools just one click away.
