import SwiftData

enum SampleDataBootstrap {
    static func seedIfNeeded(context: ModelContext) throws {
        // Intentionally empty: production data should come from user input/import, not bundled examples.
    }
}
