{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only permissions. The updater plugin is compiled out on mobile (see lib.rs cfg(desktop) + Cargo.toml cfg(not(ios/android))), so its permission must not be referenced on iOS/Android or the build fails.",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": [
    "updater:default"
  ]
}
