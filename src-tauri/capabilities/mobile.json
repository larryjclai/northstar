{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "mobile",
  "description": "Mobile-only permissions. The haptics plugin is compiled out on desktop (see lib.rs cfg(mobile) + Cargo.toml cfg(any(ios,android))), so its permission must not be referenced on desktop or the build fails.",
  "platforms": ["android", "iOS"],
  "windows": ["main"],
  "permissions": [
    "haptics:allow-impact-feedback",
    "haptics:allow-notification-feedback",
    "haptics:allow-selection-feedback",
    "haptics:allow-vibrate"
  ]
}
