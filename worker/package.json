{
  "name": "northstar-sync-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply northstar-sync --local",
    "migrate:remote": "wrangler d1 migrations apply northstar-sync --remote",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250525.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.105.0"
  }
}
