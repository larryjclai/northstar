# ─── Build artifacts ───────────────────────────────────
node_modules/
scratch/
dist/
dist-ssr/
.vite/
.cache/
coverage/
target/
src-tauri/target/
src-tauri/gen/schemas/

# ─── Tests ─────────────────────────────────────────────
test-results/
playwright-report/
playwright/.cache/

# ─── Privacy: local data ───────────────────────────────
# Tauri SQL plugin / dev SQLite / any local store
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
*.sqlite-journal
*.store
*.store-shm
*.store-wal

# Personal data exports / backups
backup/
backups/
*.bak

# Local design review uploads and screenshots. Review/redact before committing
# any images intentionally.
Design System/uploads/

# Private release-only assets. Official builds may copy these into public/
# before packaging, but they should not be published with the source repo.
private-assets/
public/bank/
public/etf-sector-feed.json

# ─── Secrets ───────────────────────────────────────────
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.mobileprovision

# ─── Logs ──────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ─── OS ────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ─── Editor / IDE ──────────────────────────────────────
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.cursor/
.fleet/

# ─── Supabase local state ──────────────────────────────
supabase/.branches/
supabase/.temp/

# ─── Legacy Swift (paused, on wip/swift-pause branch) ──
.build/
.swiftpm/
DerivedData/
*.xcuserstate
xcuserdata/

# ─── Installed Claude skills (impeccable) — local tooling, not app code ──
.claude/skills/
.github/skills/

# Local AI-agent tooling & private design history (kept locally, not published)
Design System/
.agents/
.impeccable/
.impeccable.md
skills-lock.json
