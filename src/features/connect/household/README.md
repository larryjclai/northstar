# Household Boundary

Household sharing uses a separate Household Space Key.

Users do not share whole personal vaults. Instead, selected accounts publish encrypted household projections. Private notes and receipt attachments are excluded by default unless explicitly shared.
