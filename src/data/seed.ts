import type {
  Account,
  InvestmentRecord,
  LedgerTransaction,
  PortfolioAsset,
  RecurringTransaction,
} from "../domain";

export const seedAccounts: Account[] = [];
export const seedAssets: PortfolioAsset[] = [];
export const seedInvestmentRecords: InvestmentRecord[] = [];
export const seedLedgerTransactions: LedgerTransaction[] = [];
export const seedRecurringTransactions: RecurringTransaction[] = [];
